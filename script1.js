for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x == y || x + y == 10 || y == 5 || x == 5 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( y + x >= 10 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x + y <= 10 && x >= y || x + y >= 10 && x <= y ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br><br><br><br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x == 1 || y == 1 && x < 6 || x == 5 || y == 9 && x > 5 || x == 9 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x == 9 || y == 1 || y == 9 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( y == 1 || x == 9 && y < 7 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x == 1 || x == 5 || y ==9 || y == 1 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( y == 5 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( y == 1 || y == 9 || x == y && x < 6 || x + y == 10 && x < 6 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x == 1 || x == 5 || y ==9 || y == 1 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}

document.write(`<br>`)

for( x = 1; x < 10; x++ ) {
    for( y = 1; y < 10; y++ ) {
        if( x == y || y ==9 || y == 1 ) {
            document.write(` + `)
        } else {
            document.write(` ~ `)
        }
    }
    document.write(`<br>`)
}